const card = true;
const sumCard = 500;
const sumOperation = Number(prompt())

if (card && sumOperation <= sumCard) {
    console.log("Операция выполняется")
} else {
    console.log("Операция отколнена")
}